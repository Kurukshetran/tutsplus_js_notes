(function(){
	console.log(
		Math.pow(13, 23), // Logs 13 to the 23rd power
		Math.sqrt(121), // Logs the square root of 121
		Math.round(9.13), // Rounds the way we would think it would
		Math.floor(10.67), // Rounds down always
		Math.ceil(9.11), // Rounds up always
		Math.max(75, 3, 13), // Return 75 - the highest number
		Math.min(12.2, 13.3, 12.1), // Return 12.1 - the lowest number
		Math.abs(-10), // Return 10 - distance from 0
		Math.sin(30), // Give an angle and gives sine
		Math.cos(31), // Give an angle and gives cosine
		Math.random(), //Returns number between 0 and 1
		(Math.random() * 6), // Random number between 0 and 6
		(Math.floor(Math.random() * 11)), // Random number between 0 and 10

		Math.PI, // Logs value of PI - can be used as a value
		Math.SQRT2, // Logs square root of 2
		Math.SQRT1_2 // Logs square root of 1/2
	);
}());